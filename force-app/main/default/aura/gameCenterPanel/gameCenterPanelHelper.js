({
	addToList : function(game, listType) {
		switch (listType) {
            case 'want':
                
                break;
            case 'have':
                //call apex method to add to have list
                
                break;
        }
	}
})